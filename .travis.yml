os:
  - linux
language: scala
env:
  - TEST_KIND="sbt-test"
  - TEST_KIND="s3-tests"
jdk:
  - oraclejdk8
scala:
  - 2.11.6
sudo: required
before_install:
  - sudo apt-get -y -qq update
install:
  - wget https://dl.minio.io/client/mc/release/linux-amd64/mc; sudo mv mc /usr/local/bin; sudo chmod +x /usr/local/bin/mc; mc --help
  - sudo apt-get -y -qq install python-virtualenv
script:
  - if [ "$TEST_KIND" = "sbt-test" ]; then sh travis-script/sbt-test.sh; fi
  - if [ "$TEST_KIND" = "s3-tests" ]; then sh travis-script/s3-tests.sh; fi
